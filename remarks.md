## Whole Dataset remarks

### TF-IDF model

Running with epochs > 2 when having 2 hidden layers with density of 512 and 256 Nodes relativly, does not improve the F1 score and as a result the train set was started to overfit.